name: dada2_processing_v2
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - bioconductor-dada2
  - r-base>=4.4
  - r-jsonlite
  - r-ggplot2
  # Note: cutadapt loaded via system module (cutadapt/5.0)
